services:
  - type: web
    name: weathersense-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: sh start.sh
    envVars:
      - key: OPENWEATHER_API_KEY
        sync: false
      - key: ADMIN_KEY
        sync: false
